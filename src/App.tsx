import React, { ReactElement } from 'react';

import './App.css';
import { Main } from './common/components';

const App = (): ReactElement => {
    return (
        <div className="App">
            <Main />
        </div>
    );
};

export default App;
